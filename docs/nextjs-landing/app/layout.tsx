import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FakePhoto.ai - AI Image Detection Engine",
  description: "Multi-Model AI Detection Engine - Verify if images and videos are AI-generated using OpenAI, Google Gemini, and Anthropic",
  keywords: ["AI detection", "image verification", "deepfake detection", "GPT-4 Vision", "Gemini", "Claude"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange={false}
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}